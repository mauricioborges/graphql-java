type Store {
    id: ID!
    name: String
    address: Address
    oneLineAddress: String
}

type Address {
    street: String
    city: String
    number: Int
}
type Query {
    getAllStores: [Store]!
}